[server]
protocol = http
http_addr = 0.0.0.0
http_port = 3000
domain = {{ inventory_hostname }}
root_url = %(protocol)s://%(domain)s/

[security]
admin_user = admin
admin_password = admin

[users]
allow_sign_up = false

[auth.anonymous]
enabled = false
